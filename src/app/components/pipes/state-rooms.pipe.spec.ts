import { StateRoomsPipe } from './state-rooms.pipe';

describe('StateRoomsPipe', () => {
  it('create an instance', () => {
    const pipe = new StateRoomsPipe();
    expect(pipe).toBeTruthy();
  });
});
